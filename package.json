{
  "name": "zerotask",
  "version": "1.0.0",
  "description": "Zero-dependency task runner and process orchestrator for Node.js",
  "keywords": ["task-runner", "process-orchestrator", "zero-dependency", "concurrently"],
  "license": "MIT",
  "main": "src/cli.js",
  "bin": {
    "zero": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
